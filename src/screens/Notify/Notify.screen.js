import React from 'react';
import {Text} from '../../components/common/Typography/Text.component';

const Notify = ({navigation, route}) => {
  console.log(route.params);
  return <Text>Notifyyyyyyyy</Text>;
};

export default Notify;
